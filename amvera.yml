meta:
  environment: python
  toolchain:
    name: pip
    version: 3.14.6
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  scriptName: bot.py
  containerPort: 80
  servicePort: 80
